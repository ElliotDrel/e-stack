---
context: quick
phase: 260403-bmd-build-estack-distribution
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-09T16:33:12.839Z
---

<current_state>
e-stack distribution system is fully built, reviewed, and committed to main. All code works.
Blocked on npm 2FA setup before publishing. Also need to rename GitHub repo from elliot-skills → e-stack.

Uncommitted: package.json has a minor formatting fix from `npm pkg fix` (repo URL normalization).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Restructure repo — moved skills to skills/, added estack: prefix, created package.json + bin/install.cjs - Done (commit 7a465fb)
- Task 2: Bug fixes from code review (5 bugs fixed) - Done (commit a09e75f includes .npmignore)
- Task 3: Human verify and publish - In progress, publish blocked by npm 2FA

Key artifacts:
- `bin/install.cjs` — npx installer with 3 modes (interactive, --startup, --silent)
- `package.json` — npm package "e-stack" with files whitelist
- `skills/better-title/`, `skills/chris-voss/`, `skills/github-issue-tracker/` — all with estack: prefix
- `.npmignore` — safety net excluding .planning/, .claude/, workspace dirs
- SessionStart hook auto-configured in ~/.claude/settings.json on install
</completed_work>

<remaining_work>

- Set up npm 2FA at npmjs.com/settings/~/tfa
- Run `npm publish` to publish e-stack@1.0.0
- Rename GitHub repo: elliot-skills → e-stack (GitHub Settings → General → Repository name)
- Update git remote: `git remote set-url origin https://github.com/ElliotDrel/e-stack.git`
- Update package.json repository URL to match
- Commit the package.json fix from `npm pkg fix`
- Optional: rename local folder
</remaining_work>

<decisions_made>

- npm package name: `e-stack` (estack was taken)
- Skill prefix: `estack:` (no hyphen — cleaner slash commands)
- npx-only distribution, dropped plugin marketplace (npx gives control over local change detection)
- SessionStart hook for auto-update (`npx --yes e-stack@latest --startup`)
- Local changes: backup to ~/.claude/.estack-backup/, install new version, Claude gets additionalContext to offer merge
- Interactive mode: overwrite/skip/merge/abort prompt (once for all modified files, not per-file)
</decisions_made>

<blockers>
- npm 2FA required to publish (human action needed)
</blockers>

## Required Reading (in order)
1. `bin/install.cjs` — the full installer, all 3 modes
2. `package.json` — npm package definition
3. `CLAUDE.md` — repo instructions for dev workflow

## Infrastructure State
- npm account: logged in globally (`npm login` completed)
- npm 2FA: NOT set up (blocking publish)
- GitHub repo: still named `elliot-skills`, needs rename to `e-stack`

<context>
The distribution system is done. What remains is purely operational — npm 2FA, publish, and repo rename. User also mentioned wanting to do a broader conversion renaming "elliot-skills" to "e-stack" across chat history and all references, but that's a separate task from this work.
</context>

<next_action>
Start with: Set up npm 2FA at npmjs.com/settings/~/tfa, then run `npm publish`.
</next_action>
